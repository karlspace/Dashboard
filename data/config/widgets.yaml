# =============================================================================
# SYSTEM CONFIGURATION - Homepage Dashboard Widgets
# =============================================================================
#
# Configuration file for the Homepage dashboard service widgets.
# This file defines widget configurations including:
# - Weather information
# - Search functionality
# - Date and time display
# - System resources monitoring
# - Custom logos and branding
#
# For more details, visit: https://gethomepage.dev/widgets
# =============================================================================

# - logo:
#     icon: /images/logos/bauergroup-logo-small.svg
#     href: https://go.bauer-group.com/dashboard


- openmeteo:
    label: Cham (DE)
    units: metric  # or imperial
    cache: 30      # Time in minutes to cache API responses, to stay within limits
    # Without Location the Browser uses Geolocation
    latitude: 49.2064464
    longitude: 12.6692751

- openmeteo:
    label: Tbilissi (GE)
    units: metric  # or imperial
    cache: 30      # Time in minutes to cache API responses, to stay within limits
    # Without Location the Browser uses Geolocation
    latitude: 41.7207443
    longitude: 44.7065815

- openmeteo:
    label: Singapore (SG)
    units: metric  # or imperial
    cache: 30      # Time in minutes to cache API responses, to stay within limits
    # Without Location the Browser uses Geolocation
    latitude: 1.3141703
    longitude: 103.7618499

- openmeteo:
    label: Bangkok (TH)
    units: metric  # or imperial
    cache: 30      # Time in minutes to cache API responses, to stay within limits
    # Without Location the Browser uses Geolocation
    latitude: 13.7248785
    longitude: 100.4683009


- search:
    provider: google
    focus: false  # Automatically focus the search input on page load
    showSearchSuggestions: true
    target: _blank  # One of _self, _blank, _parent or _top

# - datetime:
#     text_size: md   # 4xl, 3xl, 2xl, xl, md, sm, xs
#     locale: de
#     format:
#         dateStyle: short
#         timeStyle: short
#         #hour12: false

- worldclock:
    text_size: sm           # 4xl, 3xl, 2xl, xl, lg, md, sm, xs
    format: 24h             # 24h or 12h
    locale: de              # Override locale for date/time formatting
    layout: grid            # horizontal, vertical, or grid
    columns: 2              # columns for grid layout (1-8)
    show_date: true         # Show date (first clock's timezone)
    show_seconds: false     # Include seconds in time display
    date_position: left     # left, right, above, below
    label_bold: true        # Display label in bold
    time_bold: false        # Display time in bold
    date_format:            # Intl.DateTimeFormat options for date
      weekday: short
      day: numeric
      month: short
    clocks:
      - timezone: Europe/Berlin
        label: DE
        flag: de
      - timezone: Asia/Tbilisi
        label: GE
        flag: ge
      - timezone: Asia/Singapore
        #label: SG
        flag: sg
      - timezone: Asia/Bangkok
        label: TH
        #flag: th

# Uncomment below sections as needed
#- resources:
#    cpu: true
#    memory: true
#    disk: /

# - greeting:
#     text_size: xl
#     text: Greeting Text
